# ============================================================
# Environment Variables for Micro Journal AI
# ============================================================
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ============================================================

# ------------------------------------------------------------
# Anthropic API Configuration (Optional - for AI Insights)
# ------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/
# The app works without this for basic journaling features
VITE_ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# ------------------------------------------------------------
# Backend Proxy Configuration (Production Only)
# ------------------------------------------------------------
# Only needed if using the backend proxy server (server.js)
# ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
# PORT=3001
# FRONTEND_URL=http://localhost:5173

# ------------------------------------------------------------
# Notes:
# ------------------------------------------------------------
# - VITE_ prefix is required for frontend environment variables
# - Backend variables don't need VITE_ prefix
# - For production, use backend proxy to secure API key
# - Rebuild frontend after changing VITE_ variables
# ============================================================
